**Lemma.** $\theta^{ij}_\tau(x) = \theta_\tau(x_i - x_j)$ is convex for all $\tau > 0$.

**Proposition.** $f_{\lambda,\tau}$ is convex for all $\tau > \max\{u_{ij} : (i,j) \in K\}$.


**Proposition.** Given a simple undirected graph $G=(V,E)$, the problem of finding a $^K\text{DMGP}$ re-ordering of $G$ is NP-complete for $K > 1$, and in P for each fixed $K \geq 2$.

**Proposition.** Given a DGP instance $(K, G)$ where K = 1, $G$ has a re-order iff $G$ is connected.